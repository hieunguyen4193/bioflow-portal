process.container = 'tronghieunguyen/bcr-immunarch:latest'

profiles {
    docker {
        docker.enabled    = true
        docker.runOptions = '--platform linux/amd64'
        process.executor  = 'local'
    }
    slurm {
        includeConfig "${projectDir}/../../conf/slurm.config"
        docker.enabled    = true
        docker.runOptions = '--platform linux/amd64'
    }
    local {
        process.executor = 'local'
    }
}
